# خلفية Google Apps Script (إنتاج)

هذه هي الخلفية النشطة لهذا المشروع:

- الواجهة: Vercel (`icapphub.vercel.app`)
- الخلفية: Google Apps Script + Google Sheets
- النشر: من جذر المستودع `clasp push` (كلمة «نشر»)

لا تستخدم `/api/exec` على Vercel ولا نطاق `safety-icapp.com` — مشروع آخر.
