# نطاق الجلسة والمستودع (HSE-04-2026)

## فصل الكيان (مهم — عدم الخلط)

- المشروع **HSE-04-2026** خاص **بجهة / مجلس منظم**؛ **لا تخلط** بينه وبين مشاريع HSE أو تطبيقات أخرى من حيث: بيانات الإنتاج، جداول Google Sheets، معرفات Supabase، روابط Google Apps Script، قوائم المستخدمين، أو حقول مخصصة لجهة أخرى.
- عند الشك: اعتمد **فقط** الملفات والإعدادات **داخل هذا الـ workspace**.

## GitHub — الرفع المسموح

- المستودع الافتراضي والوحيد للرفع: **`https://github.com/YasserDiab3/ICAPP-V092026.git`** عبر **`git push origin main`** (انظر `icapp-v092026-default-repo.mdc` — قاعدة نهائية بقرار المستخدم).
- **ممنوع نهائياً** الرفع إلى `Google-App-2026-03` أو `google-TR` أو أي مستودع آخر بأي طريقة.

## ملاحظة تقنية

- قواعد Cursor تُحمَّل لكل محادثة داخل هذا الـ workspace؛ سياسة «ICAPP-V092026 فقط» و«فصل الكيان» خاصة بمشروع HSE-04-2026 حسب إعدادات المستخدم.
