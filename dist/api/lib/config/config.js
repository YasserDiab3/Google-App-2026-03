"use strict";const path=require("path"),fs=require("fs");try{require("dotenv").config()}catch{}const isVercel=!!process.env.VERCEL,ROOT_DIR=isVercel?"/tmp":path.resolve(__dirname,"../.."),DATA_DIR=path.join(ROOT_DIR,"data");if(!fs.existsSync(DATA_DIR))try{fs.mkdirSync(DATA_DIR,{recursive:!0})}catch{}const targetDbPath=path.join(DATA_DIR,"clinic_hse.db");if(isVercel){const e=[path.resolve(__dirname,"../data/clinic_hse.db"),path.resolve(__dirname,"../../data/clinic_hse.db"),path.resolve(__dirname,"data/clinic_hse.db")];for(const s of e)if(fs.existsSync(s))try{(!fs.existsSync(targetDbPath)||fs.statSync(targetDbPath).size<fs.statSync(s).size)&&fs.copyFileSync(s,targetDbPath);break}catch{}}module.exports={port:parseInt(process.env.PORT||"3001",10),host:process.env.HOST||"0.0.0.0",dbType:process.env.DB_TYPE||"sqlite",sqlitePath:process.env.SQLITE_PATH||targetDbPath,databaseUrl:process.env.DATABASE_URL||"",corsOrigin:process.env.CORS_ORIGIN||"*",sessionSecret:process.env.SESSION_SECRET||"hse_secure_local_dev_secret_2026",buildTag:"HSE_SQL_BACKEND_v1.0.0"};
